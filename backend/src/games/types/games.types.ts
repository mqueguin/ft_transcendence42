export enum GameMode {
    CLASSIC = 'CLASSIC',
    FUN = 'FUN',
  }
  
  export enum GameStatus {
    PLAYING = 'PLAYING',
    READY = 'READY',
    WAITING_FOR_PLAYER = 'WAITING_FOR_PLAYER',
    ENDED = 'ENDED',
  
    CANCELED = 'CANCELED',
  }